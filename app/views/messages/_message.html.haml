.Content
  .Content-top
    .Content-top__user-name
      = message.user.name
    .Content-top__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Content-bottom
    - if message.content.present?
      %p.Content-bottom__text
        = message.content
    = image_tag message.image.url, class: 'Content-bottom__image' if message.image.present?